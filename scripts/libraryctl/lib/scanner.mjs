// libraryctl — file scanner (V2)
// Scans the library and generates a scan report JSON.
// Planned to report: course count, material count, missing index rows,
// untracked files, possible duplicates.

export function scan(root) {
  throw new Error('[libraryctl] scan 命令尚未实现（V2 规划中）');
}
