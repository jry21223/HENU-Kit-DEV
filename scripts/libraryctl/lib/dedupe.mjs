// libraryctl — deduplication (V2)
// Identifies duplicate files by SHA256 and generates a dedupe plan.
// Duplicates are moved to 99_课程归档/01_重复文件/ and recorded
// in 00_课程档案/重复文件记录.csv.

export function dedupe(root, options = {}) {
  throw new Error('[libraryctl] dedupe 命令尚未实现（V2 规划中）');
}
