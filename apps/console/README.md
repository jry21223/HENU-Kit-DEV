# HENUKit Console

This directory is the physical application boundary for HENUKit Console. HC-03 intentionally contains only a minimal buildable Vue/Vite bundle so the legacy Study Admin cannot leak routes, Element Plus, or Study API types into the new application.

HC-04 owns the Console shell, design-system dependencies, permission states, and six-module mock overview.
