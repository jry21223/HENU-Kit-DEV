{
  "name": "final-review-platform-v2",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up --build",
    "dev:henukit": "docker compose -f docker-compose.henukit.yml --env-file .env.henukit up --build",
    "build:henukit": "docker compose -f docker-compose.henukit.yml --env-file .env.henukit build",
    "build": "pnpm run build:portal && pnpm run build:console && pnpm run build:quizcraft",
    "build:all": "pnpm run build && pnpm run build:study-legacy-admin",
    "build:web": "pnpm --filter @final-review/web run build",
    "build:portal": "pnpm --filter @henukit/portal run build",
    "build:admin": "pnpm run build:study-legacy-admin",
    "build:study-legacy-admin": "pnpm --filter @henukit/study-legacy-admin run build",
    "build:console": "pnpm --filter @henukit/console run build",
    "build:quizcraft": "pnpm --filter quiz-app run build",
    "lint": "pnpm --filter @final-review/web run lint && pnpm --filter @henukit/portal run lint && pnpm --filter @henukit/console run lint && pnpm --filter quiz-app run lint",
    "lint:all": "pnpm run lint && pnpm --filter @henukit/study-legacy-admin run lint",
    "test": "pnpm run test:libraryctl && pnpm run test:ops && pnpm run test:web-ui && pnpm run test:console && pnpm run test:quizcraft",
    "test:libraryctl": "node --test scripts/libraryctl/tests/*.test.mjs",
    "test:ops": "node --test scripts/ops/tests/*.test.mjs",
    "test:web-ui": "pnpm --filter @final-review/web run test:e2e:ui",
    "test:console": "pnpm --filter @henukit/console run test && pnpm --filter @henukit/console run test:e2e",
    "test:quizcraft": "pnpm --filter quiz-app run test:syntax"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
