{
  "name": "final-review-platform-v2",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web",
    "apps/admin"
  ],
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up --build",
    "build": "npm run build:web && npm run build:admin",
    "build:web": "npm --workspace @final-review/web run build",
    "build:admin": "npm --workspace @final-review/admin run build",
    "lint": "npm --workspace @final-review/web run lint && npm --workspace @final-review/admin run lint",
    "test": "npm run build:web && npm run build:admin",
    "test:libraryctl": "node --test scripts/libraryctl/tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
