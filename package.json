{
  "name": "final-review-platform-v2",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "docker compose -f docker-compose.dev.yml up --build",
    "build": "pnpm run build:web && pnpm run build:admin",
    "build:web": "pnpm --filter @final-review/web run build",
    "build:admin": "pnpm --filter @final-review/admin run build",
    "lint": "pnpm --filter @final-review/web run lint && pnpm --filter @final-review/admin run lint",
    "test": "pnpm run build:web && pnpm run build:admin",
    "test:libraryctl": "node --test scripts/libraryctl/tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
