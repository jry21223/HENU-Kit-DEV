# V1 源码归档已移除

`legacy/v1-next-prisma` 中的旧 Next.js + Prisma V1 应用源码已在上线前清理中从当前工作树移除。

- 该目录不是当前构建、测试或部署依赖。
- 完整历史仍保存在 Git 提交历史中，不需要在默认分支重复保存一份可执行副本。
- 需要取证或临时恢复时，应从清理提交的父提交检出，不要直接在本目录恢复生产运行。

本目录只保留此说明文件，禁止新增运行时依赖。