# HENU Kit Design System

> 状态：Baseline v2.1
> 适用范围：主站（Portal）、资料库、QuizCraft、校园生活模块及相关**面向学生的公开页面**
> 本文描述 HENU Kit 自有视觉系统，不代表河南大学官方视觉识别规范。
> 视觉基准以线上主站 `henukit.cn` 为准。
>
> **管理后台（Console）不在本基线适用范围内。** Console 是仅供内部运营人员使用的工具，
> 不是品牌触点：它的首要目标是让运营在高密度、高后果的界面上快速且准确地完成操作，
> 风格化会直接增加认知负担。Console 因此使用 shadcn-vue 的原生中性色阶，
> 不消费 `packages/design-tokens`，详见第 21 节。

新增页面和功能必须遵守本基线。确需偏离时，应在 Issue 或 PR 中说明原因、影响范围、替代方案和清理期限。

## 1. 品牌定位

HENU Kit 是面向河南大学学生的统一校园工具系统，由学生自主发起、运营和维护。

正式介绍、首页、页脚、README、登录、支付和对外宣传中必须明确：

> HENU Kit 是由河南大学学生自主发起并维护的校园工具项目，非河南大学官方产品，不代表学校官方立场。

空间有限时使用：

> 学生自主运营 · 非河南大学官方项目

禁止：

- “河南大学官方工具箱”
- “河大官方平台”
- “学校指定平台”
- “河南大学官方标准色”
- 未经证实的官方认证、授权或合作表述

## 2. 品牌原则

### 学生视角

使用“找资料”“去刷题”“今天吃什么”等任务语言，避免行政系统式命名。

### 清楚直接

页面先说明用户能做什么。主操作优先于装饰和口号。

### 有校园感，但不冒充官方

可以使用纸张、书本、档案和学生共建的视觉隐喻；不得复制或改造河南大学校徽，不使用印章式构图暗示官方身份。

### 一个系统，多种工具

子产品可独立部署，但共享品牌入口、导航、账户位置、颜色、排版、按钮、卡片、状态和页脚声明。

### 功能克制

- 主站负责发现、进入、状态和继续任务，不复制业务正文。
- 资料库不开发第二套刷题。
- QuizCraft 不承担资料归档和综合门户。
- 新功能先说明真实用户任务、唯一 Owner 和验证方式。

## 3. 色彩

视觉基准是线上主站 `henukit.cn` 的工程图纸体系：纸白底、墨色线条与文字、橙色强调。设计 token 位于 `packages/design-tokens`，**面向学生的全部前端**必须消费同一套 token。Console 不在此列（见第 21 节）。

### 强调橙（主品牌色）

| Token | 色值 | 用途 |
|---|---:|---|
| `--hk-accent` | `#FF4D00` | 主按钮、品牌标志、重点标题、选中状态、导航编号标记 |
| `--hk-accent-deep` | `#D84300` | Hover、按下、深色强调区 |
| `--hk-accent-soft` | `#FFE9DE` | 浅标签、提示背景、选中背景 |

“强调橙”是 HENU Kit 内部品牌命名，不是“河大橙”或河南大学官方标准色。

### 基础色

| Token | 色值 | 用途 |
|---|---:|---|
| `--hk-paper` | `#F2F0EA` | 页面主背景，纸白 |
| `--hk-paper-raised` | `#FFFFFF` | 卡片、弹层、输入区域 |
| `--hk-line` | `rgba(22,21,19,.14)` | 分割线、边框、禁用背景 |
| `--hk-ink` | `#161513` | 正文与主要文字（墨色） |
| `--hk-ink-muted` | `#6B6A66` | 次要说明和元信息 |

### 功能色

| Token | 色值 | 用途 |
|---|---:|---|
| `--hk-success` | `#3E7C4F` | 成功状态 |
| `--hk-warning` | `#C79A2A` | 警告状态 |
| `--hk-danger` | `#C2401F` | 危险/错误状态 |
| `--hk-info` | `#175CD3` | 信息状态 |

### 使用比例

- 纸白与白色约 70%。
- 墨色与文字约 20%。
- 强调橙和其他强调色不超过 10%。
- 每页只使用一个主要强调色。
- 美食、学习与工具模块不得发展彼此无关的主题色系统；所有颜色必须来自同一 token 集。

## 4. 排版

推荐字体（与主站一致）：

```css
/* 展示标题：Space Grotesk */
font-family: "Space Grotesk", "PingFang SC", "Microsoft YaHei", sans-serif;

/* 正文：苹方等中文字体优先 */
font-family: "PingFang SC", "Microsoft YaHei", "Space Grotesk", sans-serif;

/* 编号、标签、代码：IBM Plex Mono */
font-family: "IBM Plex Mono", "PingFang SC", "Microsoft YaHei", monospace;
```

| 层级 | 桌面端 | 移动端 | 用途 |
|---|---:|---:|---|
| Display | 64–80px | 40–48px | 首页核心标题，仅一处 |
| H1 | 40–48px | 32–36px | 页面标题 |
| H2 | 28–32px | 24–28px | 区块标题 |
| H3 | 20–24px | 18–20px | 卡片标题 |
| Body | 16–18px | 16px | 正文 |
| Meta | 13–14px | 13–14px | 标签和辅助信息 |

操作页降低标题张力，优先扫描效率。正文行宽建议不超过 72 个中文字符。等宽编号标签（如 `01 / 02 / 03`、`A-01`）是主站的工程图纸风格标记，仅用于学生端页面。

## 5. 间距与形状

- 基础单位：4px。
- 常用间距：8 / 12 / 16 / 24 / 32 / 48 / 64px。
- 工程图纸风格：以直角与细线为主，卡片圆角 4px（`--hk-radius-card`）。
- 大面积展示区块同样使用 4px 圆角，不引入大圆角卡片。
- 胶囊只用于短标签和少量动作。
- 阴影克制，优先边框、留白和背景层级；主站卡片常以 1px 墨色/纸色边框定义层级。

## 6. 全局产品外壳

所有学生端产品共享：

- HENU Kit 品牌入口。
- 一致的主导航顺序。
- 一致的登录/个人入口位置。
- 移动端底部导航规则。
- 统一非官方声明。
- 统一产品状态和错误语义。

模块名称以“HENU Kit · 学习”“HENU Kit · 校园生活”等方式出现，不建立与 HENU Kit 竞争的主品牌。

### 内容框

首页、子站页头和页面正文共用同一个内容框：最大宽度 1440px（Tailwind token `--container-site`，类名 `max-w-site`），左右留白手机 20px、`md` 及以上 32px（`px-5 md:px-8`）。页头的品牌或返回链接因此与正文第一个标题左缘对齐，从首页进入子站时品牌位置不跳。

- 不另写 `max-w-[1440px]`、`max-w-7xl` 之类的页面级宽度；需要更窄的内容（如答题区、组卷表单、结算卡片、发布表单），在内容框里再限宽，左缘仍与页头对齐。
- 例外：详情页的整页状态（加载中、找不到内容、暂时打不开）和协议正文用 `max-w-3xl` 居中成一栏，是刻意的阅读栏，不与页头左缘对齐。

## 7. 导航

桌面端一级导航不超过 5 项，移动端底部导航不超过 5 项。

推荐：

```text
首页 / 找资料 / 去刷题 / 校园生活 / 我的
```

- Logo 返回 `henukit.cn`。
- 资料库与刷题是独立任务入口。
- 资料库“去刷题”进入 QuizCraft，并携带允许的课程上下文。
- 系统内产品默认当前标签页打开。
- 外部项目显示外部标识、维护者和风险说明，可新标签页打开。

### 子站导航

资料库、刷题、美食、互助和求职雷达共用一个页头（`SubSiteNav`）：左上角是返回上一级，其后是子站字标，右侧是标签行和账户入口。

- 只有一个标签的子站（目前是资料库）不渲染标签行：没有可切换的去处，手机上只会白占一整行。
- 手机上标签行放不下时横向滑动，当前标签会被滑进可见范围。
- 暂未开放的功能保留标签位置但不可点，标签旁直接标“未开放”，不能只写在 `title` 里（触屏和键盘看不到）。读者已经在这一页时（如从收藏夹进入答题），它就是当前标签，不再标“未开放”。未开放入口是否改为隐藏，待 #540 决定。

### 首页手机菜单

`md` 以下，首页导航收进右上角的菜单按钮（`Navbar`）：

- 按钮名随开合切换为“打开菜单”/“关闭菜单”，`aria-expanded` 表明开合，`aria-controls` 指向菜单面板。
- 打开期间锁住页面滚动，面板下方铺半透明墨色遮罩；矮屏（横屏手机）放不下时面板自己滚动。
- Esc 或点遮罩关闭菜单，焦点回到菜单按钮；打开期间 Tab 只在菜单按钮和面板之间循环，遮罩下面的正文和页脚设为 `inert`，读屏软件的滑动浏览也进不去。
- 账户行与模块行一样整行可点：未登录是“登录 / 注册”，已登录是首字头像块和昵称（与模块行同字号，放得下就不截断），不另加标签。
- 窗口拉宽到 `md` 起，菜单自动收起并释放滚动锁。

## 8. 按钮

- Primary：墨色（`--hk-ink`）背景、纸白文字；Hover 转为强调橙（与主站 `button.tsx` 一致）。
- Secondary：纸白或透明背景、墨色文字、浅边框。
- Accent：强调橙背景、纸白文字（用于少量主操作或品牌强调）。
- Tertiary：纯文字。
- Danger：只用于删除、退出和不可逆操作。

同一视区原则上只有一个 Primary 动作。按钮文案使用动词，例如“查看资料”“开始刷题”“发送验证码”，避免“确定”。

## 9. 卡片

卡片必须承载明确任务，不做纯装饰容器。课程、工具和美食卡片共享圆角、边框、标题和元信息层级，仅内容结构不同。

## 10. 状态

所有产品必须覆盖：

- Loading
- Empty
- Error
- Success
- Disabled
- Login required
- Permission denied
- Offline/maintenance（适用时）

错误信息说明发生了什么和下一步。空状态提供可执行动作，不用段子替代说明。

## 11. 表单

- 输入框必须有可见标签，placeholder 不能代替标签。
- 错误靠近字段，不只用颜色表达。
- 失败后保留非敏感输入。
- 验证码支持粘贴、自动聚焦和手动修改。
- 触控区域至少 44 × 44px。
- 键盘可完成登录、搜索、筛选和提交。

## 12. 资料库“打开资料册”

保留书本/资料册创意，但作为一次性轻量进入动画：

- `600–900ms`。
- 核心层 `3–6` 个。
- 只使用 `transform` 与 `opacity`。
- 标题、搜索和主要按钮首帧可见可操作。
- 桌面端允许轻微 Hover。
- 移动端默认静态或简单淡入。
- `prefers-reduced-motion: reduce` 关闭非必要动画。
- 禁止长距离 sticky、连续翻页、多层视差和滚动期间持续位置计算进入关键路径。

完整版动效可保留为宣传录屏或设计存档，不进入用户找资料的默认流程。

## 13. 响应式与可访问性

- 移动优先，360px 宽度核心流程可用。
- 正文不小于 16px。
- 点击区域不小于 44 × 44px。
  - 纯文字链接（返回上一级、账户入口、导航与子站标签、页脚链接）用内边距或 `min-h-11` 撑满点击区；所在的行放不下时配等量负外边距，视觉尺寸和行高不变。标签的下划线挂在文字上，不随点击区下移。桌面导航在平板上同样靠手指点，也按这条做。
  - 有边框或底色的控件（筛选、切换、按钮、输入框）本身做到 44px 高，看到的范围就是能点的范围。
  - 按 WCAG 2.5.8，只有句中的行内链接（如同意告知里的《用户协议》）和装饰元素例外。首页、五个子站（含终身会员的求职雷达）和登录页在 390px 下、首页页头在 768 / 1024px 下由 `apps/portal/tests/touch-targets.spec.ts` 检查；题库目录开启时的 /practice 由 `apps/portal/tests/quizcraft-catalog.spec.ts` 检查。检查逻辑在 `apps/portal/tests/support/touch-targets.ts`。
- 手机首屏先给内容，装饰让位：
  - 子站首页（资料库、互助、刷题）在 `lg` 以下压缩标题区、隐藏右侧装饰插图，390 × 844 下第一屏能看到搜索框和至少一条内容（或加载占位）。插图隐藏后，同屏文字不再提到它。互助和刷题由 `apps/portal/tests/responsive.spec.ts` 检查，题库目录开启时的刷题另由 `apps/portal/tests/quizcraft-catalog.spec.ts` 检查，资料库的搜索与筛选由 `apps/portal/tests/library-discovery-ux.spec.ts` 检查。
  - 首页模块只在 `md` 及以上占满一屏，与吸附滚动同时启用；手机上是普通滚动，模块按内容高度排列，不留整屏空白。由 `apps/portal/tests/responsive.spec.ts` 检查。
- 正文与背景至少 WCAG AA。
- 焦点状态不得移除。
- 图片有有意义的 alt，装饰图使用空 alt。
- 动画短而克制，尊重减少动态设置。
- 不依赖 Hover 完成核心任务。

## 14. 统一账户和隐私界面

- 账户入口使用 `account.henukit.cn`（备案迁移期间 `account.superhuazai.me` 保持可用）。
- 管理后台入口使用 `console.henukit.cn`；它共享品牌归属与账户体系，但视觉上是内部工具（见第 21 节）。
- 学生邮箱验证码只证明邮箱控制权，不描述为学校认证。
- 发件域使用 `notify.henukit.cn`，发件人不得暗示官方身份。
- 验证码 5–10 分钟过期、单次使用，至少 60 秒重发等待。
- 完整邮箱、验证码、Token、Cookie 和密钥不出现在 UI 日志、错误、截图和公开仓库。
- 登录、失效、退出和跨站跳转反馈一致。
- 收集字段最小化并说明用途。

## 15. Logo

正式 Logo 尚未冻结，最低要求：

- 有可独立使用的图形符号。
- 24px 可识别。
- 不复制或改造河南大学校徽。
- 不采用容易造成官方误认的印章式构图。
- 提供 SVG、透明 PNG、单色和反白。
- 可与 `HENU Kit` 字标组合，也可用于 favicon。

旧“对不起，我是外包”字标可作为 Community Edition 彩蛋，不作为正式主标。

## 16. 非官方声明位置

| 场景 | 要求 |
|---|---|
| 首页 | 首屏或首屏后的第一个可信信息区出现完整声明 |
| 全站页脚 | 固定短版声明 |
| 登录和支付 | 操作前再次明确主体 |
| README | 项目简介后直接出现 |
| 海报和二维码 | 使用短版声明 |

声明需清晰可见，但不使用警告红或强制弹窗制造不必要风险感。

## 17. 设计交付与验收

开发前至少具备：

- 目标用户、主任务和成功条件。
- 移动端与桌面端关键页面。
- Loading/Empty/Error/Success/Disabled。
- 登录、未登录和权限不足。
- token 对照。
- 文案、数据来源和更新时间。

合并前检查：

- 手机可独立完成主流程。
- 同一视区只有一个 Primary。
- 非官方声明位置正确。
- 键盘焦点、对比度和触控尺寸合格。
- 没有复制资料库或 QuizCraft 能力。
- Mermaid 图在 DOCX/PDF 中已渲染成图像，不是源码文本。

## 18. 治理

- 本文件使用语义版本。
- 颜色、Logo、导航、账户和模块边界是核心决策，修改必须团队评审。
- 新共享组件先证明至少两个真实复用场景。
- 临时业务样式必须设清理期限。
- 设计 token 的代码实现位于 `packages/design-tokens`。

## 19. 已确定事项

- 主色称为强调橙，不称河大橙；视觉基准以线上主站为准。
- HENU Kit 是学生自主运营的非官方项目。
- HENU Kit 是统一校园工具系统，不是简单导航集合。
- 资料库只展示资料。
- QuizCraft 是唯一刷题产品。
- 子产品可以独立部署，但统一设计、账户和跳转。
- 管理后台（Console）是内部运营工具，不适用本基线；它使用组件库原生中性色阶，见第 21 节。
- 资料库保留轻量“打开资料册”。
- 计划使用 `henukit.cn`、`console.henukit.cn`、`study.henukit.cn`、`quiz.henukit.cn` 和 `account.henukit.cn`。
- 统一账户考虑河南大学学生邮箱验证码登录。

## 20. 待决策事项

- 正式 Logo。
- 域名 DNS 与证书切换时间。
- Portal 最终前端技术栈。
- 跨框架共享组件是否只保留 token，还是建立 Web Components/多实现规范。
- 统一账户首个正式接入站点和灰度规模。

## 21. 管理后台（Console）

Console 只面向内部运营人员，不是品牌触点。它的界面目标是让运营在高密度、高后果的
屏幕上快速且准确地完成操作——授予权限、停用账户、群发通知、调整积分。风格化在这里
不是加分项：它增加认知负担，也让"这一屏到底发生了什么"更难判断。

因此 Console **不适用第 3–5 节的颜色、排版与形状规则**，也不消费 `packages/design-tokens`：

- 颜色：直接使用 shadcn-vue 的原生中性色阶（`--background` / `--foreground` /
  `--muted` / `--border` 等），不引入品牌强调色。状态色仅用于表达运营状态
  （正常 / 警告 / 不可用），且必须与文字标签同时出现，不单独依赖颜色。
- 排版：系统字体栈，不加载展示字体。
- 形状：沿用组件库默认圆角，不套用主站的 4px 直角工程图纸风格。

学生端产品仍然完全遵守本基线。改动 Console 的视觉不需要按第 18 节走颜色评审，
但引入任何**新的**颜色语义（例如新增一种状态色）仍需说明理由。

Console 的品牌归属声明（"学生自主运营 · 非河南大学官方项目"）保留，见第 16 节。
