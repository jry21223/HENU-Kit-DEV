# AI Workflow

The first V2 worker uses mock LLM behavior. Real LLM integration is a later stage.

Rules:

- AI tasks are queued.
- Worker writes task results as drafts or reports.
- AI-generated questions, papers, and wiki-like content are never published directly.
- Admin or reviewer approval is required.
