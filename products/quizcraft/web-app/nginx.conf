server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Built with VITE_BASE_PATH=/quiz/ — assets live under /quiz/.
    location /quiz/ {
        try_files $uri $uri/ /quiz/index.html;
    }

    location = / {
        return 302 /quiz/;
    }
}
